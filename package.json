{
  "name": "esbuild-plugin-filelastmodified",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run build && cd test && mocha 'index.js' --no-timeout --exit",
    "dev": "esbuild ./src/index.ts --outfile=dist/index.js --watch --format=cjs",
    "build": "esbuild ./src/index.ts --outfile=dist/index.js --format=cjs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.35",
    "chai": "^4.3.4",
    "esbuild": "^0.9.6",
    "mocha": "^8.3.2",
    "typescript": "^4.2.3"
  }
}
